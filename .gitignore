# ---- Dependencies ----
# Installed packages. Huge, regenerated from package.json with `npm install`,
# never committed.
node_modules/

# ---- Environment / secrets ----
# NEVER commit these. They hold API keys and other secrets.
.env
.env.*
.dev.vars
.dev.vars.*
# Carve-out: the frontend build config is NOT secret — a localhost URL, "/api",
# and the PUBLIC VAPID key — so a fresh clone builds without a manual step.
!frontend/.env.development
!frontend/.env.production
# Carve-out: the deploy-button prompt manifest holds placeholder text only —
# it's how the button knows WHICH secrets to ask for, never their values.
# (Its absence cost the first published template its secret prompts, 19 Jul.)
!.env.example

# ---- Build output ----
# Generated artefacts, rebuilt on demand.
dist/
build/
.output/

# ---- Cloudflare / Wrangler ----
.wrangler/

# ---- OS / editor junk ----
.DS_Store
Thumbs.db
desktop.ini
*.log

template-build/
